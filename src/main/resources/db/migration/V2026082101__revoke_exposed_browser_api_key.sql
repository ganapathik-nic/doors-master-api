-- Retired migration retained to preserve Flyway history. The API-key change
-- was rolled back operationally; current credentials are managed through API
-- Client Management rather than automatic startup rotation.

